# NeuroDrive Python bridge dependencies.
#
#     pip install -r requirements.txt

# Bluetooth serial link to the MindWave Mobile 2, and the USB serial
# fallback to the ESP32. Imported lazily, so its absence is only an error
# if you actually select a serial source or transport.
pyserial>=3.5

# The webcam channel, which is how the vehicle steers. vision.py imports
# both lazily inside its own thread, so the bridge still starts without
# them; it just has no way to turn.
mediapipe
opencv-python

# The pose model is not a pip package, but it is committed to the repository
# at python_bridge/models/pose_landmarker_lite.task, so there is nothing to
# download. See vision.README.md.
